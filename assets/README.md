# assets

Place the demo capture here as **`media-toolkit-demo.gif`** (or `.png`).

The top of the repo `README.md` embeds `assets/media-toolkit-demo.gif`.
Keep that exact filename, or update the `<img src>` in the root README to match.

Recommended: a short GIF (5–10s, < 5 MB) showing the **Media Toolkit** node on
the n8n canvas with its parameters panel open. A single high-res PNG also works.
